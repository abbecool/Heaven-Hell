# Test scripts
test_*

# Build directories
build/*

# Ignore everything in run/
run/**

# But don't ignore the run/ folder itself and its subfolders
!run/
!run/Release/
!run/Debug/

# Optional: allow keeping empty folders using .gitkeep files
!run/Release/.gitkeep
!run/Debug/.gitkeep
